const items=JSON.parse(localStorage.getItem('atelie-cart-v1')||'[]'),box=document.querySelector('[data-full-cart]');box.innerHTML=items.length?items.map(i=>`<article class="full-cart-item"><img src="${i.image}" alt=""><div><h2>${i.name}</h2><p>Quantidade: ${i.qty}</p></div><strong>${i.price*i.qty} €</strong></article>`).join(''):'<p>O seu saco está vazio. <a href="/catalogo"><u>Explorar catálogo</u></a></p>';document.querySelector('[data-checkout]')?.addEventListener('click',()=>document.querySelector('[data-checkout-status]').textContent=items.length?'Checkout demonstrativo pronto para ligação a um fornecedor de pagamentos.':'Adicione primeiro um produto ao saco.');
